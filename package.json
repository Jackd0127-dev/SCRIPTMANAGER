{
  "name": "scriptmanager",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check-project.mjs && npm test",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "firebase-admin": "13.10.0",
    "zod": "4.4.3"
  },
  "engines": {
    "node": ">=20"
  }
}
